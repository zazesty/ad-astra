# Composer handoff — pointer

**Canonical file:** `/root/composer_handoff.md` on the VPS (SSOT).

This path exists so the app repo has a discoverable link; queue state lives only
at `/root/composer_handoff.md`. Do not duplicate the queue table here.
